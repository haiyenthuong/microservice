package com.order.application.command;

/**
 * Marker interface cho Command classes.
 * Các Command classes implements interface này để thực hiện ghi dữ liệu (CQRS pattern).
 */
public interface ICommand {
}
